# Careflow

Take-home assignment monorepo — Django + DRF backend, Vue + Vite frontend, PostgreSQL database.

## Prerequisites

- [Docker](https://docs.docker.com/get-docker/) & Docker Compose

## Getting started

1. **Copy the environment file**

   ```bash
   cp .env.example .env
   ```

2. **Start all services**

   ```bash
   docker compose up --build
   ```

3. **Run database migrations** (in a separate terminal)

   ```bash
   docker compose exec backend python manage.py migrate
   ```

## Services

| Service  | URL                    |
|----------|------------------------|
| Frontend | http://localhost:5173  |
| Backend  | http://localhost:8000  |
| API root | http://localhost:8000/api/ |
| Admin    | http://localhost:8000/admin/ |
| Database | localhost:5432         |

## API documentation

| UI         | URL                          |
|------------|------------------------------|
| Swagger UI | http://localhost:8000/api/docs/   |
| ReDoc      | http://localhost:8000/api/redoc/  |
| Raw schema | http://localhost:8000/api/schema/ |

Docs are auto-generated by [drf-spectacular](https://github.com/tfranzel/drf-spectacular) and update automatically as new endpoints are added.

## Project structure

```
careflow/
├── backend/       # Django + DRF
├── frontend/      # Vue 3 + Vite
├── prompts/       # AI prompt logs
├── docker-compose.yml
├── .env.example
└── README.md
```

## Useful commands

```bash
# Stop all services
docker compose down

# Wipe database volume
docker compose down -v

# Open a Django shell
docker compose exec backend python manage.py shell

# Create a superuser
docker compose exec backend python manage.py createsuperuser
```
